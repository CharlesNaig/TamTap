# TAMTAP Environment Configuration
# Copy this file to .env and update values as needed

# MongoDB Configuration
MONGODB_URI=mongodb://naig:naig1229@162.243.218.87:27017/
MONGODB_NAME=tamtap

# API Server Configuration
# Use port 80 for clean URLs (requires sudo on Pi)
# Use 4000 for development
API_SERVER_PORT=81
API_SERVER_HOST=0.0.0.0

# API URL (for hardware to call)
TAMTAP_API_URL=http://localhost:81

# Session Secret (change this in production!)
SESSION_SECRET=tamtap-group5