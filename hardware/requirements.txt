# TAMTAP v7.0 - Hardware Dependencies
# Install: pip install -r requirements.txt
# Note: Run inside virtual environment (.venv)

# Environment Variables
python-dotenv>=1.0.0

# GPIO Control for Raspberry Pi
RPi.GPIO>=0.7.1

# RFID/NFC Reader (MFRC522 via SPI)
mfrc522>=0.0.7

# I2C Communication (LCD Display)
smbus>=1.1.post2

# Face Detection (OpenCV with Haar Cascade)
# For Raspberry Pi, use opencv-python-headless (no GUI dependencies)
opencv-python-headless>=4.8.0

# MongoDB Driver (with JSON fallback if unavailable)
pymongo>=4.6.0

# Pyserial for Serial Communication (Arduino)
pyserial>=3.5
